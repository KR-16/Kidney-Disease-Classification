# Runtime dependencies are declared in pyproject.toml ([project.dependencies]).
# Installing the project in editable mode pulls them in:
-e .

# Dev/test extra (also available via the `dev` dependency group):
pytest
